# X Docker

Docker container for X

## Setup


Build the image
```
docker build -t x-image .
```

## Running
```
# docker compose (preferred)

docker compose up -d --build

# docker

docker run ...
```

## Docs
